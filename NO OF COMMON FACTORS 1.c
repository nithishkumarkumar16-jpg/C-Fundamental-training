#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int count=0,a,b;
    for(int i=1;i<=a;i++){
        if(a%i==0 && b%i==0){
          count ++;    
        }
    }

    return count;
}
